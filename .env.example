# Copy to .env and fill in (never commit .env)
# Required for Route / byllm (LiteLLM featherless_ai provider)
FEATHERLESS_API_KEY=
GITHUB_TOKEN=
NVD_API_KEY=
# Optional: override async runner self-call URL (default uses PORT on Railway, 8001 locally)
# DEPGRAPH_API_BASE=http://127.0.0.1:8080
